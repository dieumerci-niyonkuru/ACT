# --- Secrets -----------------------------------------------------------------
# Holds the database user and password in plaintext. Deploy this file manually;
# never commit it.
config/con.php

# --- Live application data (not website source) -------------------------------
# Files/ doubles as the OJS journal system's files_dir for journal.act.ac.rw
Files/

# --- Local backups, not for deployment ---------------------------------------
# Pre-optimisation originals of the site photos (~366 MB)
images_originals/

# --- Logs & runtime -----------------------------------------------------------
error_log
*.log
php_errorlog

# --- Local tooling / editor ---------------------------------------------------
.claude/
.vscode/
.idea/

# --- OS cruft -----------------------------------------------------------------
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# --- Unreferenced media archives ----------------------------------------------
# Zipped duplicates of photos already extracted under images/team/, and a video no
# page uses. Kept on disk, not in the repo (they were 121 MB of the push).
images/team/website-photos.zip
images/team/student-council.zip
documents/whatsapp-video-2024-11-26-at-12-05-19-252c3454.mp4
